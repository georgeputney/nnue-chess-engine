.venv/
__pycache__/
.mypy_cache/
.ruff_cache/
submission*.zip
*.pgn
!games/*.pgn
candidates/
snapshots/
.DS_Store
# Claude Code instructions - local working notes, not part of the portfolio
AGENTS.md
CLAUDE.md

# texel tuning: training corpus and the regenerable dataset cache
data/
tools/tune_cache*.npz
tools/tuned_tables.py

# NNUE: torch training checkpoints are regenerable from data/; only the shipped nets ship
engine/*.pt
# candidate nets from the A/B runs; only net.npz and net_eg.npz are the ones that ship
engine/net_*.npz
!engine/net_eg.npz
# the 5-man Syzygy slice, measured on 2026-09-09 and rejected (see engine/tablebase.py)
engine/syzygy5/
# eg_suite's Stockfish-labelled position cache, rebuildable with bench/eg_suite.py --build
bench/suites/
